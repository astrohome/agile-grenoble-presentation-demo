spring:
  data:
    cassandra:
      contact-points: "cassandra"
      keyspace-name: "stats"
      port: 9042
  kafka:
    bootstrap-servers: "kafka:9092"
    consumer:
      group-id: "engine"

kafka:
  topic:
    view-product: view_product
